const baseGPUValidationErrorFactory = (
  ...args: ConstructorParameters<typeof GPUValidationError>
): InstanceType<typeof GPUValidationError> => new GPUValidationError(...args);

/**
 * Factory function to create a new `GPUValidationError` object.
 *
 * A `GPUValidationError` represents an error that occurred during the validation
 * of a GPU operation. This error is typically returned when a command buffer
 * fails to execute due to invalid parameters or state.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUValidationError}
 *
 * @experimental
 *
 * @returns {GPUValidationError} A new instance of `GPUValidationError`.
 *
 * @example
 * // Example usage of createGPUValidationError
 * const validationError = baseGPUValidationErrorFactory();
 * console.log(validationError.message);
 */
export const createGPUValidationError = baseGPUValidationErrorFactory;
/**
 * Factory function to create a new `GPUValidationError` object.
 *
 * A `GPUValidationError` represents an error that occurred during the validation
 * of a GPU operation. This error is typically returned when a command buffer
 * fails to execute due to invalid parameters or state.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUValidationError}
 *
 * @experimental
 *
 * @returns {GPUValidationError} A new instance of `GPUValidationError`.
 *
 * @example
 * // Example usage of initGPUValidationError
 * const validationError = baseGPUValidationErrorFactory();
 * console.log(validationError.message);
 */
export const initGPUValidationError = baseGPUValidationErrorFactory;
/**
 * Factory function to create a new `GPUValidationError` object.
 *
 * A `GPUValidationError` represents an error that occurred during the validation
 * of a GPU operation. This error is typically returned when a command buffer
 * fails to execute due to invalid parameters or state.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUValidationError}
 *
 * @experimental
 *
 * @returns {GPUValidationError} A new instance of `GPUValidationError`.
 *
 * @example
 * // Example usage of spawnGPUValidationError
 * const validationError = baseGPUValidationErrorFactory();
 * console.log(validationError.message);
 */
export const spawnGPUValidationError = baseGPUValidationErrorFactory;
/**
 * Factory function to create a new `GPUValidationError` object.
 *
 * A `GPUValidationError` represents an error that occurred during the validation
 * of a GPU operation. This error is typically returned when a command buffer
 * fails to execute due to invalid parameters or state.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUValidationError}
 *
 * @experimental
 *
 * @returns {GPUValidationError} A new instance of `GPUValidationError`.
 *
 * @example
 * // Example usage of buildGPUValidationError
 * const validationError = baseGPUValidationErrorFactory();
 * console.log(validationError.message);
 */
export const buildGPUValidationError = baseGPUValidationErrorFactory;
/**
 * Factory function to create a new `GPUValidationError` object.
 *
 * A `GPUValidationError` represents an error that occurred during the validation
 * of a GPU operation. This error is typically returned when a command buffer
 * fails to execute due to invalid parameters or state.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUValidationError}
 *
 * @experimental
 *
 * @returns {GPUValidationError} A new instance of `GPUValidationError`.
 *
 * @example
 * // Example usage of makeGPUValidationError
 * const validationError = baseGPUValidationErrorFactory();
 * console.log(validationError.message);
 */
export const makeGPUValidationError = baseGPUValidationErrorFactory;
/**
 * Factory function to create a new `GPUValidationError` object.
 *
 * A `GPUValidationError` represents an error that occurred during the validation
 * of a GPU operation. This error is typically returned when a command buffer
 * fails to execute due to invalid parameters or state.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUValidationError}
 *
 * @experimental
 *
 * @returns {GPUValidationError} A new instance of `GPUValidationError`.
 *
 * @example
 * // Example usage of constructGPUValidationError
 * const validationError = baseGPUValidationErrorFactory();
 * console.log(validationError.message);
 */
export const constructGPUValidationError = baseGPUValidationErrorFactory;
