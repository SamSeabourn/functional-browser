const baseWebTransportFactory = (
  ...args: ConstructorParameters<typeof WebTransport>
): InstanceType<typeof WebTransport> => new WebTransport(...args);

/**
 * Factory function to create a new instance of WebTransport.
 *
 * @function createWebTransport
 * @experimental
 *
 * @returns {WebTransport} A new instance of WebTransport.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/WebTransport|WebTransport - MDN}
 *
 * @example
 * const transport = baseWebTransportFactory();
 * // Use the transport instance for communication
 */
export const createWebTransport = baseWebTransportFactory;
/**
 * Factory function to create a new instance of WebTransport.
 *
 * @function initWebTransport
 * @experimental
 *
 * @returns {WebTransport} A new instance of WebTransport.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/WebTransport|WebTransport - MDN}
 *
 * @example
 * const transport = baseWebTransportFactory();
 * // Use the transport instance for communication
 */
export const initWebTransport = baseWebTransportFactory;
/**
 * Factory function to create a new instance of WebTransport.
 *
 * @function spawnWebTransport
 * @experimental
 *
 * @returns {WebTransport} A new instance of WebTransport.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/WebTransport|WebTransport - MDN}
 *
 * @example
 * const transport = baseWebTransportFactory();
 * // Use the transport instance for communication
 */
export const spawnWebTransport = baseWebTransportFactory;
/**
 * Factory function to create a new instance of WebTransport.
 *
 * @function buildWebTransport
 * @experimental
 *
 * @returns {WebTransport} A new instance of WebTransport.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/WebTransport|WebTransport - MDN}
 *
 * @example
 * const transport = baseWebTransportFactory();
 * // Use the transport instance for communication
 */
export const buildWebTransport = baseWebTransportFactory;
/**
 * Factory function to create a new instance of WebTransport.
 *
 * @function makeWebTransport
 * @experimental
 *
 * @returns {WebTransport} A new instance of WebTransport.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/WebTransport|WebTransport - MDN}
 *
 * @example
 * const transport = baseWebTransportFactory();
 * // Use the transport instance for communication
 */
export const makeWebTransport = baseWebTransportFactory;
/**
 * Factory function to create a new instance of WebTransport.
 *
 * @function constructWebTransport
 * @experimental
 *
 * @returns {WebTransport} A new instance of WebTransport.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/WebTransport|WebTransport - MDN}
 *
 * @example
 * const transport = baseWebTransportFactory();
 * // Use the transport instance for communication
 */
export const constructWebTransport = baseWebTransportFactory;
