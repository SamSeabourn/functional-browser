const baseGPUPipelineErrorFactory = (
  ...args: ConstructorParameters<typeof GPUPipelineError>
): InstanceType<typeof GPUPipelineError> => new GPUPipelineError(...args);

/**
 * Creates a new instance of a GPUPipelineError.
 *
 * This function is a factory for generating GPUPipelineError instances, which represent errors that occur
 * during the creation or execution of GPU pipelines.
 *
 * @experimental
 *
 * @returns {GPUPipelineError} A new instance of GPUPipelineError.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUPipelineError | GPUPipelineError on MDN}
 *
 * @example
 * // Create a new GPUPipelineError instance
 * const gpuPipelineError = createGPUPipelineError();
 */
export const createGPUPipelineError = baseGPUPipelineErrorFactory;
/**
 * Creates a new instance of a GPUPipelineError.
 *
 * This function is a factory for generating GPUPipelineError instances, which represent errors that occur
 * during the creation or execution of GPU pipelines.
 *
 * @experimental
 *
 * @returns {GPUPipelineError} A new instance of GPUPipelineError.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUPipelineError | GPUPipelineError on MDN}
 *
 * @example
 * // Create a new GPUPipelineError instance
 * const gpuPipelineError = initGPUPipelineError();
 */
export const initGPUPipelineError = baseGPUPipelineErrorFactory;
/**
 * Creates a new instance of a GPUPipelineError.
 *
 * This function is a factory for generating GPUPipelineError instances, which represent errors that occur
 * during the creation or execution of GPU pipelines.
 *
 * @experimental
 *
 * @returns {GPUPipelineError} A new instance of GPUPipelineError.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUPipelineError | GPUPipelineError on MDN}
 *
 * @example
 * // Create a new GPUPipelineError instance
 * const gpuPipelineError = spawnGPUPipelineError();
 */
export const spawnGPUPipelineError = baseGPUPipelineErrorFactory;
/**
 * Creates a new instance of a GPUPipelineError.
 *
 * This function is a factory for generating GPUPipelineError instances, which represent errors that occur
 * during the creation or execution of GPU pipelines.
 *
 * @experimental
 *
 * @returns {GPUPipelineError} A new instance of GPUPipelineError.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUPipelineError | GPUPipelineError on MDN}
 *
 * @example
 * // Create a new GPUPipelineError instance
 * const gpuPipelineError = buildGPUPipelineError();
 */
export const buildGPUPipelineError = baseGPUPipelineErrorFactory;
/**
 * Creates a new instance of a GPUPipelineError.
 *
 * This function is a factory for generating GPUPipelineError instances, which represent errors that occur
 * during the creation or execution of GPU pipelines.
 *
 * @experimental
 *
 * @returns {GPUPipelineError} A new instance of GPUPipelineError.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUPipelineError | GPUPipelineError on MDN}
 *
 * @example
 * // Create a new GPUPipelineError instance
 * const gpuPipelineError = makeGPUPipelineError();
 */
export const makeGPUPipelineError = baseGPUPipelineErrorFactory;
/**
 * Creates a new instance of a GPUPipelineError.
 *
 * This function is a factory for generating GPUPipelineError instances, which represent errors that occur
 * during the creation or execution of GPU pipelines.
 *
 * @experimental
 *
 * @returns {GPUPipelineError} A new instance of GPUPipelineError.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/GPUPipelineError | GPUPipelineError on MDN}
 *
 * @example
 * // Create a new GPUPipelineError instance
 * const gpuPipelineError = constructGPUPipelineError();
 */
export const constructGPUPipelineError = baseGPUPipelineErrorFactory;
