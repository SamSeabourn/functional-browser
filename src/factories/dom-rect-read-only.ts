const baseDOMRectReadOnlyFactory = (
  ...args: ConstructorParameters<typeof DOMRectReadOnly>
): InstanceType<typeof DOMRectReadOnly> => new DOMRectReadOnly(...args);

/**
 * Factory function to create a new instance of `DOMRectReadOnly`.
 *
 * `DOMRectReadOnly` provides a way to represent a rectangle with read-only properties.
 *
 * @returns {DOMRectReadOnly} A new instance of `DOMRectReadOnly`.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/DOMRectReadOnly | MDN: DOMRectReadOnly}
 *
 * @example
 * const rect = createDOMRectReadOnly();
 * console.log(rect.x, rect.y, rect.width, rect.height);
 */
export const createDOMRectReadOnly = baseDOMRectReadOnlyFactory;
/**
 * Factory function to create a new instance of `DOMRectReadOnly`.
 *
 * `DOMRectReadOnly` provides a way to represent a rectangle with read-only properties.
 *
 * @returns {DOMRectReadOnly} A new instance of `DOMRectReadOnly`.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/DOMRectReadOnly | MDN: DOMRectReadOnly}
 *
 * @example
 * const rect = initDOMRectReadOnly();
 * console.log(rect.x, rect.y, rect.width, rect.height);
 */
export const initDOMRectReadOnly = baseDOMRectReadOnlyFactory;
/**
 * Factory function to create a new instance of `DOMRectReadOnly`.
 *
 * `DOMRectReadOnly` provides a way to represent a rectangle with read-only properties.
 *
 * @returns {DOMRectReadOnly} A new instance of `DOMRectReadOnly`.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/DOMRectReadOnly | MDN: DOMRectReadOnly}
 *
 * @example
 * const rect = spawnDOMRectReadOnly();
 * console.log(rect.x, rect.y, rect.width, rect.height);
 */
export const spawnDOMRectReadOnly = baseDOMRectReadOnlyFactory;
/**
 * Factory function to create a new instance of `DOMRectReadOnly`.
 *
 * `DOMRectReadOnly` provides a way to represent a rectangle with read-only properties.
 *
 * @returns {DOMRectReadOnly} A new instance of `DOMRectReadOnly`.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/DOMRectReadOnly | MDN: DOMRectReadOnly}
 *
 * @example
 * const rect = buildDOMRectReadOnly();
 * console.log(rect.x, rect.y, rect.width, rect.height);
 */
export const buildDOMRectReadOnly = baseDOMRectReadOnlyFactory;
/**
 * Factory function to create a new instance of `DOMRectReadOnly`.
 *
 * `DOMRectReadOnly` provides a way to represent a rectangle with read-only properties.
 *
 * @returns {DOMRectReadOnly} A new instance of `DOMRectReadOnly`.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/DOMRectReadOnly | MDN: DOMRectReadOnly}
 *
 * @example
 * const rect = makeDOMRectReadOnly();
 * console.log(rect.x, rect.y, rect.width, rect.height);
 */
export const makeDOMRectReadOnly = baseDOMRectReadOnlyFactory;
/**
 * Factory function to create a new instance of `DOMRectReadOnly`.
 *
 * `DOMRectReadOnly` provides a way to represent a rectangle with read-only properties.
 *
 * @returns {DOMRectReadOnly} A new instance of `DOMRectReadOnly`.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/DOMRectReadOnly | MDN: DOMRectReadOnly}
 *
 * @example
 * const rect = constructDOMRectReadOnly();
 * console.log(rect.x, rect.y, rect.width, rect.height);
 */
export const constructDOMRectReadOnly = baseDOMRectReadOnlyFactory;
