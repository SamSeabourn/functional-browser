const baseFloat32ArrayFactory = (
  ...args: ConstructorParameters<typeof Float32Array>
): InstanceType<typeof Float32Array> => new Float32Array(...args);

/**
 * Factory function to create a new Float32Array with the specified length.
 *
 * @function
 * @name createFloat32Array
 * @param {number} length - The length of the Float32Array to be created.
 * @returns {Float32Array} A new Float32Array instance with the specified length.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Float32Array|Float32Array - MDN}
 *
 * @example
 * const floatArray = baseFloat32ArrayFactory(5);
 * console.log(floatArray); // Float32Array(5) [0, 0, 0, 0, 0]
 */
export const createFloat32Array = baseFloat32ArrayFactory;
/**
 * Factory function to create a new Float32Array with the specified length.
 *
 * @function
 * @name initFloat32Array
 * @param {number} length - The length of the Float32Array to be created.
 * @returns {Float32Array} A new Float32Array instance with the specified length.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Float32Array|Float32Array - MDN}
 *
 * @example
 * const floatArray = baseFloat32ArrayFactory(5);
 * console.log(floatArray); // Float32Array(5) [0, 0, 0, 0, 0]
 */
export const initFloat32Array = baseFloat32ArrayFactory;
/**
 * Factory function to create a new Float32Array with the specified length.
 *
 * @function
 * @name spawnFloat32Array
 * @param {number} length - The length of the Float32Array to be created.
 * @returns {Float32Array} A new Float32Array instance with the specified length.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Float32Array|Float32Array - MDN}
 *
 * @example
 * const floatArray = baseFloat32ArrayFactory(5);
 * console.log(floatArray); // Float32Array(5) [0, 0, 0, 0, 0]
 */
export const spawnFloat32Array = baseFloat32ArrayFactory;
/**
 * Factory function to create a new Float32Array with the specified length.
 *
 * @function
 * @name buildFloat32Array
 * @param {number} length - The length of the Float32Array to be created.
 * @returns {Float32Array} A new Float32Array instance with the specified length.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Float32Array|Float32Array - MDN}
 *
 * @example
 * const floatArray = baseFloat32ArrayFactory(5);
 * console.log(floatArray); // Float32Array(5) [0, 0, 0, 0, 0]
 */
export const buildFloat32Array = baseFloat32ArrayFactory;
/**
 * Factory function to create a new Float32Array with the specified length.
 *
 * @function
 * @name makeFloat32Array
 * @param {number} length - The length of the Float32Array to be created.
 * @returns {Float32Array} A new Float32Array instance with the specified length.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Float32Array|Float32Array - MDN}
 *
 * @example
 * const floatArray = baseFloat32ArrayFactory(5);
 * console.log(floatArray); // Float32Array(5) [0, 0, 0, 0, 0]
 */
export const makeFloat32Array = baseFloat32ArrayFactory;
/**
 * Factory function to create a new Float32Array with the specified length.
 *
 * @function
 * @name constructFloat32Array
 * @param {number} length - The length of the Float32Array to be created.
 * @returns {Float32Array} A new Float32Array instance with the specified length.
 *
 * @see {@link https://developer.mozilla.org/en-US/docs/Web/API/Float32Array|Float32Array - MDN}
 *
 * @example
 * const floatArray = baseFloat32ArrayFactory(5);
 * console.log(floatArray); // Float32Array(5) [0, 0, 0, 0, 0]
 */
export const constructFloat32Array = baseFloat32ArrayFactory;
